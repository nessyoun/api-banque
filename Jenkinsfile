pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        git 'https://github.com/nessyoun/api-banque.git'
      }
    }
    stage('Display system date') {
      steps {
        sh 'date'
}
}
}
}
